# CF-BACKEND requirements — Phase 1 (merged with live repo)
# Existing (keep exactly — these match the live backend):
fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
numpy<2.0
chromadb==0.5.0
sentence-transformers==3.0.1
requests==2.32.3
pydantic==2.9.2
python-multipart==0.0.9
supabase>=2.0.0
pydantic[email]>=2.0.0

# ── NEW for Phase 1 ──────────────────────────────────────
reportlab>=4.0.0      # CF Computer branded PDF generation (pdf_generator.py)
jinja2>=3.1.0         # Email templates (email_automation.py)
